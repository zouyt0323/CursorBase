---
description: 本项目通用编码与协作规范
globs: 
alwaysApply: true
---

# 项目通用规范

本规则作为示例，展示 `.cursor/rules` 下 `.mdc` 文件的用法。

## 作用

- 为 AI 提供持久、可复用的项目级上下文。
- 可配置 `alwaysApply: true` 使每次对话都附带，或通过 `globs` 仅对匹配文件生效。

## 可填写内容示例

- 技术栈与版本约定
- 代码风格（命名、缩进、注释）
- 目录与模块划分约定
- 提交信息、PR 规范等

修改本文件或新增其他 `.mdc` 即可扩展项目规则。
